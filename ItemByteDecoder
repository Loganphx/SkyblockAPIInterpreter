import nbt
import io
import base64


def decode_item_bytes(raw_data):
   data = nbt.nbt.NBTFile(fileobj = io.BytesIO(base64.b64decode(raw_data)))
   print(data.pretty_tree())

def get_count(raw_data):
   t = decode_inventory_data(raw_data[0]['item_bytes'])
  return(t['i'][0]['Count'])
